import 'package:flutter/material.dart';

class Explore {
  String? title;
  String? content;
  Color bgColor = Colors.blue;
  String iconPath = "";

  Explore(this.title, this.content, this.bgColor, this.iconPath);
}
